// packages/app/server/api/window.js

// Этот файл намеренно оставлен пустым.
// Он нужен только для того, чтобы наш API-шлюз (api.js) и система автодополнения
// знали о существовании модуля "window".
// Реальная логика для команд "window.minimize", "window.close" и т.д.
// перехватывается и обрабатывается напрямую в ядре (packages/core/main.js).

module.exports = {};