{
  "name": "new-longday-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "start": "npm run start --workspace=app",
    "build": "npm run build --workspace=app",
    "package": "npm run package --workspace=app"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "electron": "37.2.6",
    "electron-builder": "26.0.12"
  }
}